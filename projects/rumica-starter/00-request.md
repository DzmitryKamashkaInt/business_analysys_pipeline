# Project Request — Rumica Starter (Phase 1 of `artdecor-home-design`)

## Original Request
Analyze how the full Rumica MVP scope (from the `artdecor-home-design` project) can be trimmed down to a starter/Phase-1 version covering: **catalogs (furniture AND building materials — the latter is a new addition, not in the original scope), user account/login, and product ordering** — while preserving a clear path to add the remaining previously-scoped features (2D planner, floor-plan recognition, galleries, favorites, subscriptions, admin panel, analytics) in later phases.

This is run as its own project (slug `rumica-starter`) because it is a materially different, smaller deliverable (Phase 1) derived from the already-finalized `artdecor-home-design` Vision & Scope and Architecture Specification — not a rework of that finalized MVP scope itself. The full MVP documents remain untouched in `projects/artdecor-home-design/ready_for_dev_docs/` as the target for later phases.

## Source Context (carried over from the parent project)

**Parent project:** `projects/artdecor-home-design/` — finalized Vision & Scope + Architecture Specification (Bitrix24-centric thin-bridge architecture), plus ad hoc note `notes_20260824_122706.md` (starter-scope analysis, produced on request before this project was created).

**Parent Vision & Scope — Full Rumica MVP scope (for reference, not all of it carries into Starter):**
Purpose: web platform connecting home-interior planning with real, purchasable furniture/decor products — differentiator is direct in-platform purchase of items used in a design. Target users: B2C home-design enthusiasts (primary), design-minded professionals (secondary, same tiers), internal Admin staff.
Full MVP in-scope: auth (email/password + Google OAuth), Free/Paid tiers, 2D room planner (Three.js), floor-plan upload+recognition+dimension-confirmation, Flat Plans gallery, public Design Projects gallery (publish/duplicate), product catalog (browse/search/filter/detail/"place in interior"), Favorites, Admin catalog CRUD (manual+CSV+pricing/priority), project management (save/duplicate/export/share/publish), cart+custom checkout form→Bitrix24 Payment APIs, subscription payment via Bitrix24-hosted flow, "My Orders" (Bitrix24 CRM), Admin panel (dashboard/user mgmt/moderation/reports), analytics (internal+GA/Mixpanel), transactional email.
Full MVP Business Requirements: BR-1 (non-specialists plan+furnish with real products), BR-2 (direct purchase from a design — core differentiator), BR-3 (freemium growth model), BR-4 (centralize commerce/CRM/catalog in Bitrix24), BR-5 (community/publish loop).
Parent architecture direction: Bitrix24-centric — Bitrix24 hosts admin/catalog/CRM/payments; thin custom bridge layer (API Gateway/BFF, Auth Bridge, Commerce Bridge with two distinct payment paths, Catalog Sync/Proxy — webhook + periodic reconciliation cache due to Bitrix24 standard-plan API limits, Projects Service with its own DB+Object Storage, Plan Recognition Adapter, Export Service, Analytics Forwarder).

## Already-Resolved Decisions (from ad hoc analysis + explicit user confirmation) — authoritative, do not re-ask

1. **Building materials catalog — CONFIRMED as a separate catalog** with its own attribute schema, not a category/filter within a single shared furniture catalog.
2. **Curated design packs — deferred.** Starter checkout covers individual products only, from either catalog. Design packs are out of Starter scope entirely (tied to the planner, which is also deferred).
3. **CSV import for catalog seeding — required from day one**, not optional/manual-only.
4. **Starter functional scope carried forward from the parent project's finalized FRs/UCs** (see full mapping in the parent project's `notes_20260824_122706.md`):
   - Carried into Starter (adapted where noted): FR-1, FR-2, FR-3 (auth, unchanged); FR-4-starter (profile, simplified — no tier visibility); FR-13-starter (catalog browse/search/filter — **now spans two independent catalogs**, furniture and building materials, each with its own attribute schema); FR-15-starter (Admin catalog CRUD, manual entry **and CSV import from day one**, for both catalogs; pricing/priority rules deferred); FR-20 (cart + custom Rumica checkout form → Bitrix24 Payment APIs, unchanged); FR-22 (My Orders from Bitrix24 CRM, unchanged).
   - Use cases carried into Starter: UC-1 (register/login, unchanged); UC-6-starter (purchase — **individual products only**, from either catalog; no design packs); UC-7 (My Orders, unchanged); UC-9-starter (Admin — catalog seeding/CRUD for both catalogs only, no user management/moderation/reports).
5. **Explicitly deferred to a later phase** (not deleted, not "Post-MVP" in the original sense — just out of Starter, to be re-added in a future phase without rework of what Starter builds): 2D Room Planner (FR-6–FR-10, UC-2, UC-4), floor-plan upload/recognition, Flat Plans gallery (FR-11), public Design Projects gallery/publish/duplicate (FR-12, FR-19, UC-3, UC-5 in part), Project management/save/duplicate/export/share (FR-17–FR-19, UC-5), Favorites (FR-16, UC-10), Free/Paid subscription tiers and their limits (FR-5), subscription checkout path (FR-21, UC-8), full Admin panel beyond catalog CRUD (FR-23–FR-26), analytics integration (FR-27).
6. **Business-requirement tension — carry forward as a known, accepted limitation, not to be silently resolved:** a catalogs+account+ordering Starter does NOT satisfy BR-2 (purchase *from a design* — Rumica's core differentiator) or BR-5 (community/publish loop) — both depend entirely on the deferred planner. BR-1 is only partially met (no "plan" capability in Starter). BR-3 (freemium) does not apply to Starter (no tiers). Only BR-4 (Bitrix24 centralization) is fully satisfied by Starter. This should be stated plainly in the Starter Vision & Scope's Purpose/Business Requirements section, not hidden.
7. **Architecture direction:** Starter must remain Bitrix24-centric, consistent with the parent project — no new backend paradigm. **Open question for the system-architect step (Step 4 of this pipeline run, not pre-decided here):** given building materials is now confirmed as a genuinely separate catalog with its own attribute schema (not a shared-schema category/filter), should this be modeled as (a) two catalog entities within Bitrix24 (each with its own property set), or (b) some other Bitrix24-native multi-catalog mechanism; and whether the existing Catalog Sync/Proxy design (webhook + periodic reconciliation cache) generalizes to two catalogs via parameterization, or needs two parallel sync pipelines feeding a common or separate front-end read model. This is a real architectural decision to be made during the architecture step, not assumed by this request document.

## Q&A Transcript
(To be appended by the orchestrating agent as the business-analyst's and system-architect's clarifying questions — if any remain beyond what's already resolved above — are answered.)

**Q:** [Discovery] With furniture and building materials as two independent catalogs (each with its own attribute schema), should Starter provide a single unified search/browse across both, or fully separate storefront sections with no cross-catalog search? → **A:** Unified search bar across both catalogs; category browsing/filtering stays catalog-specific (separate browse sections).

**Q:** [Discovery] Since the two catalogs have distinct attribute schemas, should CSV import use one template per catalog, or a single unified template? → **A:** Two separate CSV templates — one for furniture, one for building materials, each matching that catalog's own attribute schema.

**Q:** [Discovery] Should Admin catalog management (FR-15-starter, UC-9-starter) be a single unified admin screen with a catalog-type switcher, or two separate admin sections? → **A:** Unified — one "Catalog Management" screen with a toggle between Furniture and Building Materials, shared CRUD + CSV import UI pattern.

**Q:** [Discovery] Can a single cart/order contain items from both catalogs at once, or must furniture and building materials be ordered separately? → **A:** Mixed cart allowed — one cart, one checkout, one Bitrix24 order can contain items from both catalogs together.

**Q:** [Discovery] Is there an expected initial/near-term scale for the two catalogs that Starter's CSV import and browse/filter UX should be designed around? → **A:** Medium (low thousands of SKUs per catalog) — import should support batch/async processing; listing needs solid pagination and indexed filtering.

**Q:** [Post-draft review] The draft Vision & Scope only implied viewing a product's photos/details in passing (inside FR-5/FR-6's "view product detail" wording, and as one unlinked step of UC-2), with no explicit, dedicated requirement or use case for the item card itself. Should this be made explicit? → **A:** Yes — add an explicit requirement and dedicated use case for the item/product detail card (image gallery + full attribute set per catalog schema). Applied as FR-14 (Starter) and UC-6 (Starter): View Item Card (Photos and Details); UC-2 and the corresponding User Flow updated to reference UC-6; architecture specification updated to note the Catalog Read Model as the source for item-card images (as Bitrix24-hosted URL references) and attributes, with a dedicated UC-6 sequence diagram.

**Q:** [Evaluation round 1] The Vision & Scope's "Out of Scope (b, Post-MVP for whole product)" list (AI features, Seller role, Shopify integration, multi-currency) is not traceable to this project's request or Q&A — it was pulled in from the parent project's Post-MVP list without confirmation for Starter. Keep, remove, or soften? → **A:** Remove — Out-of-Scope narrows to only the "(a)" deferred-to-later-phase list that is explicitly confirmed in this document; the itemized Post-MVP list is dropped from this document entirely.

**Q:** [Evaluation round 1] The Architecture Specification names "Bitrix24 Cloud Standard Plan" and uses its specific API rate limits as the justification for the Catalog Sync/Proxy + Catalog Read Model design, but no Q&A entry or request text in this project confirms that plan tier (only the parent project's summary does). Keep, soften, or verify? → **A:** Soften — replace the specific "Standard Plan" naming with a generic reference to Bitrix24 API rate limits, without asserting a specific plan tier as confirmed for this project.

**Q:** [Evaluation round 1] The Vision & Scope explicitly states the BR-2/BR-5-not-satisfied, BR-1-partial, BR-3-n/a tension (per decision #6, "stated plainly, not hidden"), but the Architecture Specification's Overview mentions only BR-4 and is silent on the rest. Should the architecture doc restate the full tension? → **A:** Keep as-is — the Vision & Scope remains the sole owner of the full Business-Requirement tension statement; the Architecture Specification does not need to restate it.

**Q:** [Evaluation round 2] Round-1's Bitrix24 plan-tier softening was explicitly confirmed only for the Architecture Specification; it was also proactively applied to the Vision & Scope's "Presumed Architecture Direction" section (same underlying phrase) without a separate confirmed answer. Keep, revert, or log a confirming decision? → **A:** Keep as-is — accepted as a natural extension of the round-1 decision; no document change, this entry closes the traceability gap.

**Q:** [Evaluation round 2] After removing Out-of-Scope subsection (b) in round 1, only "(a) Deferred to a later phase of Rumica" remains, with no paired (b). Keep the "(a)" label, drop it, or replace with a plain heading? → **A:** Replace with a plain heading — "Deferred to a later phase of Rumica" without the "(a)" prefix. Applied.

## Major Rework — Custom Stack + Multi-Country Payments (2026-08-24)

Following two ad hoc notes (`notes_20260824_151012.md` — Bitrix24-vs-custom architecture conflict found when cross-checking an external development estimate; `notes_20260824_152118.md` — BR/FR/UC coverage of that estimate under a "switch to custom backend" hypothesis), the user has decided to actually adopt the custom-backend direction and, on top of that, introduce a materially new multi-country payment requirement. This section documents that decision and the full set of clarifying answers resolving it. **This supersedes the project's prior Bitrix24-centric direction — both `ready_for_dev_docs/` documents are to be reworked in place, not forked into a new project**, since this is still the same Starter phase, just with a changed implementation direction and an expanded payment requirement.

### Resolved decisions

1. **Backend architecture — full switch away from Bitrix24.** Catalog, authentication/identity, and CRM/orders all become custom-built (in the shape the external estimate assumed: a Java/Spring-Boot-style service with its own relational schema), replacing every Bitrix24-hosted responsibility from the prior architecture. Bitrix24 is dropped entirely — not partially retained for any function.
2. **Payment countries — all 6 confirmed in scope:** Kazakhstan, Uzbekistan, Armenia, Georgia, Belarus, Russia.
3. **Payment architecture — orchestration layer, not a single gateway.** A payment orchestration layer inside the custom backend exposes one internal interface (createPayment, getStatus, refund, webhook) behind which multiple country-specific provider adapters plug in. Per-country primary local providers, confirmed as **working assumptions pending vendor PoC/validation** (not final vendor commitments — same posture as CubiCasa in the parent project):
   - Kazakhstan → Kaspi Pay + fallback bank card acquiring.
   - Uzbekistan → Payme + Click, both offered in parallel.
   - Armenia → Ameriabank vPOS or VPOS.am (multi-bank aggregator).
   - Georgia → TBC E-Commerce API or Bank of Georgia (Visa/Mastercard acquiring).
   - Belarus → bePaid (ЕРИП + cards).
   - Russia → ЮKassa/CloudPayments (contingent on continued legal presence).
   Routing is by the cart's country/currency context (not a single global checkout), and the adapter boundary must allow swapping any single country's provider within weeks, not a checkout rewrite — per the regulatory/compliance instability named for the region.
4. **Currency model — true multi-currency.** Catalog, cart, and checkout display and store prices in each country's own local currency; no single reference currency with provider-side conversion.
5. **Language/UI — stays single-language (Russian)** for all 6 countries. Multi-language localization is explicitly NOT part of this rework; only currency/payment-routing varies by country.
6. **Previously identified estimate-coverage gaps are to be closed properly in the reworked documents** (these are gaps in a THIRD-PARTY ESTIMATE against already-confirmed requirements, not new scope decisions — restate them correctly, don't reopen them): Google OAuth (FR-2) must be explicit; the two catalogs (Furniture, Building Materials) must keep their own independent attribute schemas end-to-end (including two separate CSV import templates, FR-10) rather than collapsing into one undifferentiated schema; the item card (FR-14/UC-6) must render per-catalog-schema attribute sets, not one generic shape; UC-5's per-row CSV import success/failure reporting and UC-6's alternative scenarios (image placeholder when none on file; omit blank/broken attributes rather than rendering them) must be explicit requirements, not implied.
7. **Previously identified scope-creep items — resolved individually:**
   - Role hierarchy (Editor/Viewer beyond Admin): **rejected** — Starter keeps the single Admin role only, as before.
   - Stock/inventory management (stock quantity, decrement on paid order, out-of-stock handling): **accepted into scope** — add as a new FR/NFR.
   - Guest cart with merge-on-login: **accepted into scope** — add as an explicit extension of FR-11/UC-3 (anonymous users may build a cart pre-login; it merges into their account cart on login/registration).
   - Nested category hierarchy + breadcrumb navigation: **accepted into scope** — add as an explicit extension of FR-8.
8. **BR-4 requires rewording, not retirement-by-omission.** BR-4 was "centralize commerce/CRM/catalog in Bitrix24 — fully satisfied by Starter." With Bitrix24 dropped, this BR must be rewritten to reflect what actually replaces it as a rationale (e.g., owning the full stack to control cost/timeline and to support the country-specific payment-provider abstraction that a third-party platform couldn't accommodate) — this is the business-analyst's call on exact wording, but the BR must not simply vanish or be left describing Bitrix24.

### Task for this rework cycle
Rework `working/vision-and-scope.md` and `working/architecture-specification.md` in full to reflect all of the above, run them through evaluation, and finalize to `ready_for_dev_docs/`, replacing the Bitrix24-centric versions.

**Q:** [Rework — business-analyst clarifying] Are both catalogs sold identically in all six countries, or can availability differ by country? → **A:** Same catalog everywhere — every SKU purchasable in every country; only price/currency and payment routing vary by country.

**Q:** [Rework — business-analyst clarifying] Is stock (FR-15) a single global pool per SKU, or per-country pools? → **A:** Single global pool per SKU, shared across all six countries.
